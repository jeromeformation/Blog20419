{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création compte entreprise</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary">Créer compte entreprise</button>
    {{ form_end(form) }}
{% endblock %}

